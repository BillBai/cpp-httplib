# cpp-httplib 架构文档

## 1. 概述

`cpp-httplib` 是一个仅包含单个头文件的 C++ HTTP/HTTPS 库。其设计的核心理念是简洁易用，开发者只需将 `httplib.h` 文件包含在项目中即可快速构建 HTTP/HTTPS 应用。该库不依赖于任何第三方库（除非需要 SSL/TLS 支持），并具有跨平台的特性。

## 2. 核心架构

`cpp-httplib` 的架构主要围绕 `Server` 和 `Client` 两个核心类展开，分别用于实现 HTTP/HTTPS 服务器和客户端功能。

### 2.1. Server 类

`Server` 类是 `cpp-httplib` 的基础，用于创建和管理 HTTP 服务器。其主要职责包括：

*   **监听和连接管理**：通过 TCP 套接字在指定的主机和端口上进行监听，并接受客户端连接。
*   **路由**：提供 `Get`、`Post`、`Put`、`Delete` 等方法，允许用户将特定的 HTTP 请求路径和方法与处理函数（Handler）进行绑定。
*   **请求处理**：当接收到请求时，`Server` 类会解析请求行、头信息和请求体，然后调用匹配的路由处理函数。
*   **响应生成**：处理函数通过 `Response` 对象设置响应的状态码、头信息和响应体，`Server` 类负责将其格式化为标准的 HTTP 响应并发送给客户端。
*   **线程模型**：`Server` 内部使用线程池来处理并发请求，每个客户端连接都在一个独立的线程中进行处理，以确保服务器的高性能和响应能力。

### 2.2. Client 类

`Client` 类用于发起 HTTP 请求。其主要功能包括：

*   **请求构造**：提供 `Get`、`Post` 等方法，方便用户构造不同类型的 HTTP 请求，并可设置请求头、请求体、URL 参数等。
*   **连接管理**：自动处理与服务器的 TCP 连接，包括建立连接、发送请求和接收响应。
*   **响应解析**：接收到服务器响应后，会将其解析为 `Response` 对象，方便用户获取状态码、头信息和响应体。

### 2.3. SSL/TLS 支持

`cpp-httplib` 通过 `SSLServer` 和 `SSLClient` 两个类提供对 HTTPS 的支持。这两个类分别继承自 `Server` 和 `Client`，并利用 OpenSSL 库实现加密通信。

*   `SSLServer`：在 `Server` 的基础上，增加了 SSL/TLS 证书和私钥的加载和配置，从而实现 HTTPS 服务器。
*   `SSLClient`：在 `Client` 的基础上，增加了对服务器证书的验证，确保与服务器的安全通信。

## 3. 请求处理流程

`cpp-httplib` 的请求处理流程如下：

1.  **连接建立**：服务器通过 `listen` 方法在指定端口上监听，客户端通过 `Client` 类与服务器建立 TCP 连接。如果是 HTTPS，则在此阶段进行 SSL/TLS 握手。
2.  **请求接收和解析**：服务器接收到客户端的 HTTP 请求，并逐行解析请求行和头信息。
3.  **路由匹配**：服务器根据请求的路径和方法，在预先注册的路由表中查找匹配的处理函数。
4.  **处理函数执行**：找到匹配的处理函数后，服务器将解析得到的 `Request` 对象和用于构造响应的 `Response` 对象传递给处理函数。开发者在处理函数中实现业务逻辑。
5.  **响应发送**：处理函数执行完毕后，服务器将 `Response` 对象中的内容格式化为标准的 HTTP 响应，并通过 TCP 连接发送回客户端。
6.  **连接关闭或保持**：根据 HTTP 协议的 `Connection` 头（Keep-Alive 或 close），服务器决定是保持连接以处理后续请求，还是关闭连接。

## 4. 模块化与扩展

`cpp-httplib` 通过 C++ 的宏定义实现了一定程度的模块化。例如，用户可以通过定义 `CPPHTTPLIB_OPENSSL_SUPPORT` 来启用 SSL/TLS 功能。类似地，也支持通过 `CPPHTTPLIB_ZLIB_SUPPORT` 和 `CPPHTTPLIB_BROTLI_SUPPORT` 来启用 Gzip 和 Brotli 压缩。

这种设计使得库本身非常轻量，用户可以根据实际需求选择性地开启所需功能，避免了不必要的依赖和代码膨胀。

## 5. 总结

`cpp-httplib` 以其简洁的设计、易于集成的单头文件形式，以及强大的功能，为 C++ 开发者提供了一个优秀的 HTTP/HTTPS 解决方案。其清晰的 `Server`/`Client` 架构、基于线程池的并发模型以及模块化的功能支持，使其在快速开发和高性能应用场景中都表现出色。
